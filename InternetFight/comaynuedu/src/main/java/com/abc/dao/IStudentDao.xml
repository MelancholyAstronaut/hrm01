<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.dao.IStudentDao">
    <select id="verify" parameterType="int" resultType="student">
        select * from student where id=#{userId}
    </select>
    <select id="queryStudents" parameterType="String" resultType="student">
        select * from student where adminclass=#{adminclass}
    </select>
    <select id="queryStudent" parameterType="String" resultType="student">
        select * from student where name=#{name}
    </select>
    <select id="querypersonallearn" parameterType="int" resultType="personallearn">
        select * from personallearn where studentid=#{id}
    </select>
</mapper>