<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Dao.husbandDao">
    <resultMap type="po.husband" id="husbandAndWife">
        <id property="hid" column="h_id"/>
        <result property="hname" column="h_name"/>
        <association property="wife" javaType="po.wife">
            <id property="wid" column="w_id"/>
            <result property="wname" column="w_name"/>
        </association>
    </resultMap>
    <select id="find" parameterType="int" resultMap="husbandAndWife">
       select h.id h_id,h.`name` h_name,w.id w_id,w.`name` w_name FROM husband h,wife w WHERE h.wife_id = w.id AND h.id = #{id}
     </select>
</mapper>